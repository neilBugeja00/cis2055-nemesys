@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    ViewData["Title"] = "Investigation Entry";
}
@model InvestigationClass

<div class="container">
        <h3 class="display-4">Details</h3>
        <div class="row">
            <div class="col-md-6">
                <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
                    <ol class="carousel-indicators">
                        
                        @*@for (int i = 0; i < Model.Gallery.Count(); i++)
                        {
                            <li data-target="#carouselExampleIndicators" data-slide-to="@i" class="@(i==0 ? "active" : "") "></li>
                        }
                    </ol>
                    <div class="carousel-inner">
                        @for (int i = 0; i < Model.Gallery.Count(); i++)
                        {
                            <div class="@(i==0 ? "carousel-item active" : "carousel-item") ">
                                <img class="d-block w-100" src="@Model.Gallery[i].URL" alt="@Model.Gallery[i].Name">
                            </div>
                        }*@

                    </div>
                    <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                        <span class="sr-only">Previous</span>
                    </a>
                    <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                        <span class="sr-only">Next</span>
                    </a>
                </div>
            </div>
            <div class="col-md-6">

                <hr />

                <ul class="list-group">
                    <li class="list-group-item"><span class="font-weight-bold">Investigation Description      - </span> @Model.InvestigationDescription</li>
                    <li class="list-group-item"><span class="font-weight-bold">Investigation Date      - </span> @Model.InvestigationDate</li>
                    <li class="list-group-item"><span class="font-weight-bold">Investigator First Name  - </span> @Model.InvestigatorFirstName</li>
                    <li class="list-group-item"><span class="font-weight-bold">Investigator Last Name    - </span> @Model.InvestigatorLastName</li>
                    <li class="list-group-item"><span class="font-weight-bold">Investigator Email      - </span> @Model.InvestigatorEmail</li>
                    <li class="list-group-item"><span class="font-weight-bold">Investigator Mobile   - </span> @Model.InvestigatorMobile</li>
                    
                </ul>
    </div>
    <div>
        <a asp-action="Index">Back to Home</a>
    </div>
</div>